La traitement des images
	Pour cette tâche, je l'ai divisé en deux parties à traiter : une pour analyser le color, et autre sur le border, et à la fin, je les combine en une class globale.
	Le fichier test1.py est pour analyser les colore dans un images de cube.
	Le fichier test2.py est pour analyser les borders, et renvoyer une table.
